In this directory is a class to be used as logger message formatter.

# About ECSFormatter

This class will encapsulate the log into a JSON data structure with various default element to be appended in log.